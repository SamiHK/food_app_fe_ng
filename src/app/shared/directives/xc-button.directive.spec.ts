import { XcButtonDirective } from './xc-button.directive';

describe('XcButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new XcButtonDirective();
    expect(directive).toBeTruthy();
  });
});
